import { NotFoundContent } from '@/components/motion/not-found/content';

export default function NotFound() {
   return (
      <div className="flex min-h-dvh w-full items-center justify-center">
         <NotFoundContent />
      </div>
   );
}
