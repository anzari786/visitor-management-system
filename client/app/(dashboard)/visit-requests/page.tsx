import { Content } from '@/components/shared/content';

export default function VisitRequestsPage() {
   return (
      <Content
         subtitle={
            <p>
               Review incoming visit requests and move them through approval.
            </p>
         }
      />
   );
}
